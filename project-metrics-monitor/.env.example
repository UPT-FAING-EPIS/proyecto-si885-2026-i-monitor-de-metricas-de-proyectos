# Copia este archivo a `.env` para ejecucion local.
# El proyecto lee automaticamente solo `GITHUB_TOKEN` desde `.env`.
# Los valores `GITHUB_OWNER`, `GITHUB_REPOS`, `ETL_SINCE`, `DB_PATH` y `EXPORT_DIR`
# se configuran en la CLI, GitHub Actions o Render segun el entorno.
GITHUB_TOKEN=github_pat_replace_me
